using System;
class owow {
  public static void Main() {
    
  }
  public static bool Primo(int x) {
    double lim = Math.Sqrt(x);
    int k = 2;
    while (k < lim) {
      
      if (x % k == 0)
    }
  }
}